# Live 1 fingerprint (public HTTP only)

This folder describes **https://brodansh.de.com.eg** without touching it.

- `fingerprint.json` — committed snapshot of edition, database names, and headers.
- `fingerprint.runtime.json` — written by `./scripts/fingerprint-live1.sh` (gitignored).

Live 1 databases: `brodan`, `brodan2026`, `brodansh`, `test`. Edition: Odoo **18.0+e**.

No dump, passwords, or Enterprise addons are stored here.
